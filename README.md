# Domain Driven Design with PHP and Symfony
Этот проект демонстрирует пример реализации Domain Driven Design (DDD) с использованием PHP и Symfony.

## Описание
Domain Driven Design (DDD) - это подход к разработке программного обеспечения, который фокусируется на сложной логике домена и позволяет создавать более поддерживаемые и гибкие приложения. В этом проекте показана реализация DDD на примере сущности User.

## Автор книги
Эрик Эванс. Предметно-ориентированное проектирование (DDD). Структуризация сложных программных систем

## Структура проекта
Проект следует стандартной архитектуре DDD с четырьмя основными слоями:

1. **Domain Layer** - содержит описание сущностей и порты взаимодействия
2. **Application Layer** - координирует работу между слоями
3. **Infrastructure Layer** - обеспечивает технические детали (базы данных, внешние сервисы)
4. **Presentation Layer** - отвечает за взаимодействие с пользователем

## Задачи проекта
- [X]Создание структуры слоев DDD
- [X]Реализация сущности Shop. Доменный слой
- [X]Создание репозитория для сущности Shop. Инфраструктурный слой
- [X]Разработка сервисов для работы с Shop. Приложения слой
- [X]Реализация контроллеров представления для работы с Shop. Представления слой
- [X]Реализация Unit тестирования для сущности Shop

## Запуск проекта
### С использованием Docker (рекомендуется)
1. Убедитесь, что у вас установлен Docker и Docker Compose
2. Запустите проект:
   ```bash
   docker-compose --env-file .env.local up -d
   ```

3. Откройте в браузере: http://localhost:8081/
### Остановка проекта
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Работа с контейнерами
Для выполнения команд внутри PHP контейнера:
```bash
docker compose exec myapp_php bash
```

## 🧪 Тестирование
Запуск тестов:
```bash
php bin/phpunit
```

## Структура приложения Магазин в DDD
```bash
    src/
    --shop/
        --application/             # слой управления приложением
           --services/             # бизнес-логика
        --domain/                  # слой предметной области
           --entity/               # сущноси
           --value/                # значения
           --repository/           # интерфейсы репозиториев
           --services/             # интерфейсы бизнес-логики
        --infrastructure/          # слой инфраструктуры
           --repository/           # Репозитории - работа с данными
           --mapper/               # Маппинг dto
        --presentation/            # слой представления. вход/выход для api
            --http                 # http запросы
                --controller/      # Контроллеры
            --mapper               # интерфесы Маппинга
            --responses/           # DTO response
        shared/                    # Общие классы для всего приложения